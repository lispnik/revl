# build artifacts
/revl
/tvlisp

# macOS
.DS_Store

# fasl caches
*.fasl
.cache/

# ocicl download cache (restored from ocicl.csv by `ocicl install`)
/ocicl/

# NOTE: ocicl.csv (the dependency pin manifest) IS committed.
# systems/revision and systems/revision-term are committed symlinks to the sibling
# ../revision and ../revision-term projects (not on ocicl).  Ignore everything else.
/systems/*
!/systems/revision
!/systems/revision-term
