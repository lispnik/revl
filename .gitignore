# build artifact
/tvlisp

# fasl caches
*.fasl
.cache/

# ocicl bookkeeping (regenerated on `ocicl install`)
/systems.csv

# NOTE: systems/tvision is a committed symlink to the sibling ../tvision
# project (tvision is not on ocicl).  Ignore everything else under systems/.
/systems/*
!/systems/tvision
